machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t mariadb:10.0 .
test:
  override:
    - docker run -d -p 13306:3306 -e MARIADB_PASS="test" mariadb:10.0; sleep 20
    - mysqladmin -uadmin -ptest -h127.0.0.1 -P13306 ping | grep -c "mysqld is alive"
